/**
 * constants/index.ts
 *
 * Barrel for app-specific, non-visual constants.
 * For design tokens (colors, fonts, radii, spacing) import from `theme` instead.
 */
export { ComponentSize, IconSize } from './AppSizes';
export { AppStrings } from './AppStrings';
export { AppConfig } from './AppConfig';
