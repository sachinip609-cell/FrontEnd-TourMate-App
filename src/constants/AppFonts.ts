/**
 * AppFonts
 *
 * Re-exports font tokens from the theme layer for backward compatibility.
 * Source of truth: `src/theme/tokens.ts`
 *
 * Prefer importing directly from `theme` in new code:
 *   import { FontFamily, FontWeight, LetterSpacing } from '../../theme';
 */
export { FontFamily, FontWeight, LetterSpacing } from '../theme/tokens';
